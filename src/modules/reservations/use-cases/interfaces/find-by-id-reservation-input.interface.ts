/**
 * @fileoverview Input interface for finding a reservation by ID and user.
 *
 * @interface find-by-id-reservation-input
 */
export interface IFindByIdReservationInput {
  id: string;
  userId: string;
}
