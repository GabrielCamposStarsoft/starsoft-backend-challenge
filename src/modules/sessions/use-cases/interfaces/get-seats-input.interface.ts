/**
 * Input for retrieving seats for a session.
 * @interface IGetSeatsInput
 * @property {string} sessionId - The unique identifier of the session.
 */
export interface IGetSeatsInput {
  sessionId: string;
}
