/**
 * @fileoverview Input interface for invalidating a refresh token.
 *
 * @interface invalidate-refresh-token-input
 */
export interface IInvalidateRefreshTokenInput {
  refreshTokenValue: string;
}
