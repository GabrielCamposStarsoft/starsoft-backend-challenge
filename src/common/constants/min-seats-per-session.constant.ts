/**
 * @fileoverview Minimum number of seats required per session.
 *
 * Business rule from challenge: each session must have at least 16 seats
 * to accept reservations.
 *
 * @constant min-seats-per-session
 */
export const MIN_SEATS_PER_SESSION: number = 16;
